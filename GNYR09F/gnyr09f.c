#include <stdio.h>

int
adjBC (int n) {
    int ret = 0;
}

int
main (int argc, char *argv[]) {
    int testcases = 0;
    int id = 0;
    int number = 0;
    int bit_length = 0;
    int ret = 0;
    scanf ("%d", &testcases);
    while (testcases--) {
        scanf("%d %d %d", &id, &number, &bit_length);
    }
    return 0;
}
